import Link from "next/link";import { workouts } from "@/lib/workouts";import styles from "@/components/member-section.module.css";export default function WktJourney(){return <main className={styles.page}><Link className={styles.back} href="/app/programas">← MY PROGRAMS</Link><section className={styles.head}><span>ACTIVE PROGRAM</span><h1>WKT Militar</h1><p>21 sessões guiadas preservadas do produto original. A agenda de 12 semanas será persistida no Supabase.</p></section><section className={styles.grid}>{workouts.map(w=><article className={styles.card} key={w.id}><span>MISSION {String(w.id).padStart(2,"0")}</span><b>{w.code}</b><small>{w.focus}</small><Link href={`/app/workout/${w.slug}`}>START →</Link></article>)}</section></main>}